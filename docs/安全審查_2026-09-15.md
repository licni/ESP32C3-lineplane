# 安全審查 2026-09-15(第二次)

GG 要求:全面檢查啟動,待命,取消,馬達關停等邏輯,以及備份功能與其他功能的潛在衝突.
代理一次只派一項小工作,每段回來立刻記在這裡. 判斷原則:使用者可預期且要確認的行為(例如停用安全開關)視為正常,不算問題.

審查對象:2026-09-15 目前工作樹(起飛油門,熱點密碼,選組/分區備份碼,套用取消項目,「測試」組名稱鎖定,暖色主題).
上一次審查:docs/安全審查_2026-09-14.md.

## 分段

| 段 | 範圍 | 狀態 |
|---|---|---|
| 1 | flight.cpp 狀態機:待機/等待放穩/倒數/取消/結束,安全開關,自動倒數 | 已回報 |
| 2 | 馬達關停路徑:control.cpp,esc_output/esc_service,緊急停止,撞擊,感測器故障,OTA,電變校正 | 已回報 |
| 3 | 備份碼與飛行/設定/WiFi 的互動:鎖定,未儲存,套用時機,名稱鎖定,active 切換 | 已回報 |
| 4 | 網頁 API 的鎖定涵蓋:哪些 POST 在起飛程序/飛行中被擋,哪些沒擋 | 已回報 |
| 5 | 板上實測(我自己做) | 已做 |

## 發現

### 第 1 段:飛行狀態機(代理只讀審查,2026-09-15)

| # | 嚴重度 | 位置 | 問題 | 觸發情境 | 處理 |
|---|---|---|---|---|---|
| 1-1 | 中 | flight.cpp:456 | 撞擊斷電鎖只看「狀態 = 已結束且原因撞擊」. 撞擊後按網頁開始被拒(未儲存/韌體待確認)→ 狀態變待機,鎖解除,之後撿飛機推到門檻就照常進起飛程序 | 撞擊 → 改設定沒存 → 按開始被拒 → 扶飛機推到 | 已修(獨立旗標 crashLocked) |
| 1-2 | 中 | flight.cpp:325,372,641 + 468 | 馬達停止(緊急停止/降落結束)同一拍就接受推力手勢,沒有冷卻;手勢重臂 1.5 秒是相對上一次手勢,不是相對馬達停止 | 停止當下飛機剛好有 ≥ 手勢門檻的機頭方向加速度且水平 → 進等待放穩 → 放穩 1 秒 + 開關已按過就倒數 | 已修(停止後 2 秒封鎖,板上驗證) |
| 1-3 | 低 | flight.cpp:430 | 上電自動倒數等待中把啟動手勢改成開啟(不存),自動倒數取消但 autoStartUsed 仍 false,改回關閉也不會再等,狀態顯示不一致 | 手勢關 → 上電等開關 → 網頁開手勢 → 放棄 | 已修(標自動倒數已用,事件 28/6) |
| 1-4 | 低 | flight.cpp:458,469,479 | 推到門檻後 1.5 秒內按網頁「開始」,請求被靜默丟掉但網頁回成功 | 待機時推飛機後馬上按開始 | 已修(網頁/序列請求不受 1.5 秒重臂) |
| 1-5 | 低 | flight.cpp:528 | 序列測試指令 disturb 在外力設「不理會」時旗標殘留到下一趟 | 只影響序列測試 | 已修(不理會時清旗標) |
| 1-6 | 低 | flight.cpp:589,597 | 起飛油門沒和油門上下限交叉驗證,超出時被夾,時間軸圖畫設定值 | 起飛油門 80% 上限 50% | 已修(時間軸文字提示) |

待查證(要 GG 決定是否算非預期):
- 起飛油門維持期間補償照加(只看 noCompSec). 網頁寫「維持起飛油門」.
- 上電自動倒數等開關/等放平期間網頁沒有取消手段,只能拔電或等上限.
- refuseIfLocked 到真正寫入之間數十微秒的競爭視窗,機率極低.

代理確認正確的路徑:tryStart 三項拒絕都回待機;取消五條路都回待機且旗標重設;armLatched 每趟重賦值;等待上限按過就不計時;外力延長上限 +10 秒;自動倒數只給真上電;撞擊 2 拍;finish 同拍輸出 0;緩啟動/起飛油門/換段/降落邊界(0 秒,除零)都正確;快照加鎖,寫入路徑都經 refuseIfLocked.

### 第 2 段:馬達關停與輸出路徑(代理只讀審查,2026-09-15)

每拍輸出決策鏈(control.cpp:186~255):讀感測器 → 校正計時 → 手動心跳(≤0.5 秒且上一拍待機/結束且非校正)→ flightUpdate(estop/撞擊/觸地/保險同拍決定 motorOn)→ 輸出:校正中送 max(只在 FS_ARMING)/ 手動且 !motorOn 送手動值 / 其他 motorOn ? 換算 : min. 代理結論:沒有任何路徑在狀態機說停時輸出非零;待機輸出非零只有 A.

| # | 嚴重度 | 位置 | 問題 | 觸發情境 | 處理 |
|---|---|---|---|---|---|
| 2-A | 中 | control.cpp:241~253, settings.cpp:210 | 待機時網頁把「油門 0% 脈寬」escMinUs 拉高(不必儲存,不必勾拆槳,不必解鎖手動輸出),下一拍就輸出新脈寬,PWM 電變馬達直接轉. 網頁說明沒警語 | PWM 電變已解鎖,待機,網頁把 escMinUs 從 1000 調到 1300 | 已修(要先勾拆槳,加警語) |
| 2-B | 中 | esc_output.cpp:38~47,144~191;sdkconfig 無 esp_task_wdt_add | 控制工作若卡死(非例外),三種協定的訊號都由硬體/計時器維持最後值,馬達停在最後油門;優先權最高 loop 也餓死,緊急停止連不上;任務看門狗沒監看任何工作. 例外走 panic 重開是安全的 | 驅動層死鎖(目前無忙等,I2C 有 10ms 逾時,機率低) | 已修(任務看門狗 5 秒,板上驗證飛行/寫快閃不誤觸發) |
| 2-C | 低 | main.cpp:401, esc_output.cpp:264~276 | escBegin 失敗被忽略,狀態機照飛,網頁顯示油門正常但腳位沒訊號 | LEDC/RMT 建立失敗 | 已修(拒絕原因 10 + 開機事件) |
| 2-D | 低 | flight.cpp:479, main.cpp:164 | 序列 gesture 在非待機狀態不清旗標,飛完那拍被受理 | 只有 USB 序列 | 已修(同 4-B1) |
| 2-E | 低 | wifi_manager.cpp:436~443, esc_service.cpp:73~83 | 通電第 5 秒 bootcnt 歸零寫 NVS,校正旗標 10 秒過期寫 NVS,可能落在上電自動倒數/起飛程序中,控制迴圈停頓數 ms~數十 ms(記 EV_LOOP_LATE),輸出由硬體維持 | 手勢關閉上電自動倒數 | 不修(低,輸出由硬體維持) |
| 2-F | 低 | main.cpp:433/438 | fwUpdateBegin 在 controlBegin 之後,與註解不符;FS_ARMING 3 秒後才讀 fwBlock 所以目前無害 | — | 已修(fwUpdateBegin 移到控制工作前) |

待查證:G 手動輸出「從最低開始」檢查有 5ms 級競態(極低);H 網頁上傳韌體中斷 busyFlag 可能卡住(安全側,要拔網路實測);I ArduinoOTA.onStart 內 ESP.restart 視窗(極低);J 手動輸出中第二台裝置按開始顯示成功但沒動作;K 序列 orient/save/txp/wifi 沒有飛行中鎖定(USB 限定).

代理逐題結論:緊急停止 ≤1 拍;撞擊 2 拍(10ms);感測器故障飛行中不停馬達(規格);韌體更新/WiFi 重啟/校正旗標都不碰輸出;手動輸出五種離開方式都上鎖,0.5 秒逾時回最低;校正旗標先清再校正,軟體重開取消,10 秒時效,只在 PWM,校正中擋手動/OTA/重開/手勢,只有「設旗標 10 秒內拔電放幾天再接」會不知情執行(已註明);開機 escPinLow 第一行,無浮接空窗;上電自動倒數最快通電 8 秒起飛(armSwitchOff=1 + 手勢關閉時不需任何操作,GG 09-14 的決定,出廠 armSwitchOff=0);飛行中所有設定 API 被擋,快照不變.

### 第 3 段:備份碼與其他功能(代理只讀審查,2026-09-15)

| # | 嚴重度 | 位置 | 問題 | 觸發情境 | 處理 |
|---|---|---|---|---|---|
| 3-A | 中 | settings_backup.cpp:406,438~443,482~496 | 舊世代碼(09-14 產生)套用所選分區/風格時,碼裡沒有的「新欄位」變出廠值而不是保持板上值(設計文件原本就這樣寫,但和網頁「碼裡沒有的部分不動」矛盾) | 世代 1 碼套用「安裝」→ buzzerLow 回 0(蜂鳴器電位反轉);套用風格 → takeoffHold 回 0 | 已修(解碼從板上值起算,板上驗證) |
| 3-B | 中 | settings_backup.cpp:512~521 | 含 WiFi 的碼:先 settingsApplyImage 再 wifiSaveConfig;NVS 寫入失敗時飛行設定已套進 RAM(含 IMU 方位即時切換),網頁只顯示「WiFi 寫入失敗」 | NVS 寫入失敗(罕見) | 已修(先存 WiFi 再套) |
| 3-C | 中 | wifi_manager.cpp:232~239 | 已在 3 分鐘 WiFi 試用中再套用含 WiFi 的碼,接著按「保持」,新 WiFi 沒試用過就固定(handleWifiSet 同樣路徑,既有行為) | 重開後 3 分鐘內套 WiFi 碼再按保持 | 已修(存了未重開的設定後按保持只停計時,試用保留) |
| 3-D | 低 | settings_backup.cpp:405 | 套用的組從出廠影像起算,reserved/pts[4] 不是板上值,memcmp 判未儲存;無功能影響 | 套自己的碼回自己 | 已修(同 3-A) |

待查證:E 未選分區的浮點值經步進對齊可能差 1 ulp 而顯示未儲存(建議 img 先整份 = live);F 六組全選套用切換 active 未儲存時監看頁曲線可能閃爍(純顯示);G 飛行中 check 只讀,幾 ms 負載.

代理確認正確:apply 走 refuseIfLocked(涵蓋等放穩/等開關/倒數/運轉/降落),check 無副作用(static 每次重設);TOCTOU 視窗無害(快照在進入時取,dirty 就拒);WebServer 單執行緒,兩網頁操作序列化;pendingActive 非全選 = live.active → -1,放棄可完整還原,WiFi 不退回網頁有寫;SHARED_PARAMS 41 個 key 全在 SHARED_SECTIONS,sectionOf=0 走保留 live(安全側);名稱鎖定 major 1/≥2 對齊,位元先讀完才略過,開機不 dirty;WiFi 區塊長度/緩衝/跳過都有擋;mask=0&sec=0,act≥6,點數/角度都擋在 apply 前;buf[640] 在 loop 堆疊(最長碼約 540);產生端 dirty 拒絕所以碼 = 已儲存值.

### 第 4 段:網頁 API 與序列指令的鎖定涵蓋(代理只讀審查,2026-09-15)

代理列了全部 35 條 HTTP 路由與 38 條序列指令的表(擋法:refuseIfLocked 擋等放穩/倒數/運轉/降落但不擋手動輸出與校正;controlOtaAllowed 擋起飛程序/運轉/手動/校正但不擋更新中). 結論:**HTTP 端點在起飛程序與運轉中沒有能改到飛行輸出或設定的漏洞**;會改設定的序列指令全部 USB 限定,真飛行不接 USB 無影響.

| # | 嚴重度 | 位置 | 問題 | 觸發情境 | 處理 |
|---|---|---|---|---|---|
| 4-B1 | 中 | flight.cpp:72~74,479,559 | reqGesture 只在待機/結束分支尾端清除;等放穩/倒數/運轉中送來的手勢請求會留到落地進 DONE 後自動再啟動起飛程序(等放穩 → 倒數 → 馬達再轉). 序列 gesture 直接命中;/api/start 的狀態預檢讀上一拍快照,與推飛機手勢同一 5ms 內也會留住 | 飛行中送 gesture / 極短視窗 | 已修(非待機一律清 reqGesture,板上驗證) |
| 4-B2 | 中 | fw_update.cpp:380~384, flight.cpp:209~213 | fwBlock 只在 tryStart 檢查;controlOtaAllowed 通過到 fwUpdateStarted 之間(<5ms)若手勢成立,等放穩/倒數不再看 fwBlock,倒數完在寫快閃中啟動馬達 | 按更新的同一拍推飛機 | 已修(等待放穩/倒數中 fwBlock 就取消) |
| 4-B3 | 低 | flight.cpp:528 | 同 1-5 | — | 已修(同 1-5) |
| 4-B4 | 低 | main.cpp:127 | 序列 scanall 掃 GPIO3(收輪舵機)與 GPIO21(安全開關),Wire.end 後 GPIO3 不還給 LEDC;只在沒感測器時能用 | USB 限定 | 不修(USB 限定,沒感測器才能用) |
| 4-C | 低 | web_server.cpp:343 | /api/cancel 在運轉中回 ok 但不動作(網頁只在起飛程序顯示取消鈕,所以只有直接打 API 會誤判) | — | 已修(回 cancelstate) |
| 4-D | 低 | flight.cpp:420 | 手動輸出中按 /api/start,請求被清且不記拒絕原因也不記事件(同 2-J) | 第二台裝置 | 已修(回 startbusy) |
| 4-E | 低 | web_server.cpp:482 | /api/reboot 在板子背景下載韌體中沒擋(重開打斷下載,分區不變,安全);也不查未儲存變更 | — | 不修(安全側) |
| 4-F | 低 | web_server.cpp:499 | /update 在「新韌體待確認」時允許上傳,會寫到舊版所在分區,退回時退到剛被覆蓋的那份 | — | 不修(低) |

手動輸出中沒擋的寫 NVS 端點(save/select/wifi/keep/backup apply/calib off):寫快閃時控制迴圈停頓,PWM 與單向 DShot 由硬體持續,雙向 DShot 漏幾框(電變 failsafe 是停轉方向),代理判低,可接受.

無認證(既知類別)的新延伸:熱點密碼可改 → 區網內任何人可改密碼 + 重開 + 保持,把飛主鎖在外,只剩開關電 3 次救援(中);armSwitchOff + /api/start 讓整條啟動鏈不需要人在飛機旁(高,但是 GG 09-14 的決定,網頁有置頂警告). /api/wifi GET 本來就回傳密碼,備份碼含 WiFi 沒有新增洩漏面.

序列指令飛行中會改東西的(全部 USB 限定):orient(立即換軸,補償錯,高),sim(設計如此),reboot(序列版沒 controlOtaAllowed),rpmtest(停送雙向 DShot 訊框 23ms),dshot(GPIO4 建 RX 並忙等 1 秒),save/wifi/txp/stadrop/powerontest/rescuetest/armsw/armwait.

### 第 5 段:板上實測(2026-09-15,COM20 開發板,沒接馬達;未儲存變更先記下放棄,測完放回)

腳本:scratchpad safety5.py(序列 gesture/armsw/estop + HTTP),紀錄 test_logs/safety5_20260915_180736.txt. 通過 19,「缺陷確認」2.

| 項目 | 結果 |
|---|---|
| 2-A 待機時 /api/set escMinUs=1100 不儲存 | **電變輸出立即變 1100µs**(確認);放棄後回 1000 |
| 飛行中 GET /api/backup,POST /api/backup/check | 可用,只讀,沒有未儲存變更 |
| 飛行中 backup/apply,/api/set | 都回 locked |
| 4-C 飛行中 /api/cancel | 回 ok 但仍在飛行(確認) |
| **4-B1 飛行中序列 gesture → estop** | **確認缺陷**:狀態走向 已結束 → 等待放穩 → 倒數(安全開關覆寫 armsw 1 仍在,放穩 1 秒就倒數). 第一次跑時腳本沒抓到已結束那拍,板子已在倒數,手動取消 |
| **1-2 estop 後 100ms 內 gesture** | **確認缺陷**:已結束 → 等待放穩 → 倒數,沒有冷卻 |
| 收尾 | 六組與共用設定與測前相同,WiFi 不變,沒有未儲存變更,armsw 覆寫已清 |

沒測(要改儲存的設定):1-1 撞擊鎖(crashEnable 板上 0),4-B2 韌體寫入同拍競態(<5ms 視窗).

## 修正(2026-09-15,GG 決定全部修)

flight r24/h r14,control r21,esc_output,main,web_server r29,settings_backup r9,wifi_manager r9,web_page r75. 編譯通過,USB 燒錄 COM20.
板上驗證(safety5.py 重跑,24/24 通過):飛行中送手勢 → 緊急停止後不再進起飛程序;停止後 100ms 手勢被擋,2 秒內網頁開始回 startblock,2 秒後手勢照常受理;
飛行中取消回 cancelstate;基準碼(舊格式)只套測試組時起飛油門兩項保持板上值,共用設定不變;寫 NVS(select)後看門狗沒重開;測完設定與測前相同.
沒實測(只靠程式碼):撞擊鎖獨立旗標(要開撞擊斷電並模擬撞擊),等待放穩/倒數中韌體寫入取消,WiFi 3-C(要存 WiFi 並重開),電變掛載失敗拒絕.
不修:2-E(NVS 寫入停頓,輸出由硬體維持),4-B4(USB 限定),4-E/4-F(安全側或低).
