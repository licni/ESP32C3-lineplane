// 版本流水號: r1 (2026-09-25) 初版(GG):帶螢幕板 0.42 吋 OLED(72x40)顯示油門,動力階段,起飛程序,取消/拒絕原因,WiFi 與 IP
#pragma once
#include <Arduino.h>
// ============================================================================
// 帶螢幕板(pins_config.h BOARD_C3_OLED = 1)的 OLED. 普通版兩個函式都是空的.
// 只在 Arduino loop 呼叫:螢幕與 MPU6050 共用 GPIO5/6 的 I2C,U8g2 每次最多傳 25 位元組(約 0.6ms),
// 控制工作讀感測器時最多等一次傳輸. 畫面每 0.2 秒重畫,內容沒變就不傳.
// 螢幕傳輸連續失敗就停用到重新上電,不讓壞掉的螢幕一直佔用感測器的線.
// ============================================================================

void displayBegin();                // Wire.begin 之後呼叫
void displayTick(uint32_t nowMs);
